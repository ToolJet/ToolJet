export type SourceOptions = {
  username: string;
  password: string;
  database: string;  
  port: string;
};
export type QueryOptions = {
  operation: string;
  doc_name: string;
  record_id: string;
  body: string;
  rev_id:string;
};
