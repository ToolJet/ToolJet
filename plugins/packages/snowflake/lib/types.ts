export type SourceOptions = {
  username: string;
  account: string;
  database: string;
  warehouse: string;
  schema: string;
  role: string;
  password: string;
};
export type QueryOptions = {
  query: string;
};
