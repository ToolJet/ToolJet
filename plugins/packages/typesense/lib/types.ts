export type SourceOptions = {
  host: string;
  port: string;
  protocol: string;
  api_key: string;
};
export type QueryOptions = {
  operation: string;
  schema: string;
  collection: string;
  searchParams: string;
  document: string;
  id: string;
};
