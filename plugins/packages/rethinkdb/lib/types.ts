export type SourceOptions = {
  username: string;
  database: string;
  port: string;
  host: string;
  protocol: string;
  ssl: string;
  password: string;
};
export type QueryOptions = {
  operation: string;
};
