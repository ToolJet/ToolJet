{
  "name": "mongo",
  "version": "1.0.0",
  "main": "lib/mongo.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "build": "tsc -b",
    "clean": "rimraf ./dist && rimraf tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "common": "^1.0.0",
    "mongodb": "^4.2.2",
    "rimraf": "^3.0.2"
  }
}
