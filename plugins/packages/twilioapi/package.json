{
  "name": "twilioapi",
  "version": "1.0.0",
  "main": "lib/twilioapi.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "build": "tsc -b",
    "clean": "rimraf ./dist && rimraf tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "common": "^1.0.0",
    "rimraf": "^3.0.2",
    "twilio": "^3.72.0"
  }
}
