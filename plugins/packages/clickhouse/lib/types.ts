export type SourceOptions = {
  port: string;
  username: string;
  password: string;
  host: string;
  database: string;
  format: string;
  protocol: string;
};
export type QueryOptions = {
  operation: string;
  query: string;
};
