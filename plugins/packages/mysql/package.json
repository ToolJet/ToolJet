{
  "name": "mysql",
  "version": "1.0.0",
  "main": "lib/mysql.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "build": "tsc -b",
    "clean": "rimraf ./dist && rimraf tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "common": "^1.0.0",
    "knex": "^0.95.14",
    "rimraf": "^3.0.2"
  }
}
