export type SourceOptions = {
  access_token: string;
};
export type QueryOptions = {
  operation: string;
  channel: string;
  message: string;
  sendAsUser: string;
  limit: string | number;
  cursor: string;
};
