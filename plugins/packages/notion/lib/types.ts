export type SourceOptions = { token: string };
export type QueryOptions = {
  resource: string;
  operation: string;
  database_id: string;
  page_id: string;
  filter: any;
  sorts: any;
  limit: any;
};
