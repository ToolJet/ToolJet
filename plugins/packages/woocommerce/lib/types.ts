export type SourceOptions = {
  consumer_key: string;
  consumer_secret: string;
  host: string;
};
export type QueryOptions = {
  operation: string;
  body: any;
  product_id: string;
  order_id: string;
  customer_id: string;
  resource: string;
};
