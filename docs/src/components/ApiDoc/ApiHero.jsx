import React from 'react';

export default function ApiHero() {
  return (
    <div className="api-hero">
      <span className="api-hero__eyebrow">REST API</span>
      <h1 className="api-hero__title">ToolJet API Reference</h1>
      <p className="api-hero__subtitle">
        Manage users, workspaces, applications, and more — all programmatically.
        Integrate ToolJet into your CI/CD pipelines, identity providers, and internal tooling without ever touching the UI.
      </p>
    </div>
  );
}
