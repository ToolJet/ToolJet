# Server Test Suite Audit Ledger

Generated as part of the 2026-07-17 server test suite health audit.
Phase A rows are findings only — fixes land in Phase C, one task per row (or tight file group).

Baseline run: `NODE_ENV=test NODE_OPTIONS='--max-old-space-size=8192' npx jest --config jest.config.ts --verbose --maxWorkers=50% --testPathPatterns='test/modules/.*/unit/.*\.spec\.ts'`
(the brief's `--testPathPattern` flag is renamed to `--testPathPatterns` in the installed Jest version; used the corrected flag). Full log: `/tmp/unit-baseline.log`.

Result: **Test Suites: 4 failed, 22 passed, 26 total. Tests: 17 failed, 2 skipped, 346 passed, 365 total.**

Note: the task brief's stated scope was "64 unit spec files" — the actual count under `server/test/modules/*/unit/*.spec.ts` on this branch is **26 files**. All 26 were run and/or read; the ledger below reflects the real file set, not the brief's stale estimate.

## Phase A — Unit Suite (`server/test/modules/*/unit/*.spec.ts`)

| File | Symptom | Suspected Root Cause | Recommendation |
|------|---------|----------------------|-----------------|
| `platform-git-sync/unit/push-path-resolution.spec.ts` | 4 failing tests: `TypeError: The "path" argument must be of type string. Received undefined` in "app not in a folder" (2 its), "app inside a folder" (1 it), and "should not throw while resolving a 100-character name" (1 it) | `PlatformGitPushService.resolveAppPath` (`ee/platform-git-sync/push.service.ts:279`) takes 3 params — `(app, repoPath, displayName)` — but the spec's `resolveAppPath` test helper (line 30-31) calls `service.resolveAppPath(app, REPO)` with only 2 args, so `displayName` is `undefined` and `path.join(resourceDir, undefined)` throws. API drift: the method gained a `displayName` param (to source the name from `app_versions.app_name` per branch) that the test wasn't updated for. | fix-in-place |
| `app/unit/app-auth.guard.spec.ts` | 6 failing tests, all `TypeError: this.appRepository.findAppBySlug is not a function` — "when app is not found › throws NotFoundException", "when workspace is archived › throws BadRequestException", "when app.isPublic is true" (2 its), "when app.isPublic is false" (2 its) | `AppAuthGuard.canActivate` (`src/modules/apps/guards/app-auth.guard.ts:36`) now calls `this.appRepository.findAppBySlug(slug)`, but the spec's `mockAppRepository` (line 10, 36) only stubs `{ findOne: jest.fn() }` — no `findAppBySlug`. Repository lookup method was renamed/added on the production side without updating the test double. | fix-in-place |
| `tooljet-db/unit/tooljet-db-import-export.service.spec.ts` | `.export › should throw NotFoundException for non-existent table` — expected a rejected promise, got `null` resolved | `TooljetDbImportExportService.export()` (`src/modules/tooljet-db/services/tooljet-db-import-export.service.ts:34-36`) has an explicit, commented backward-compatibility change: it now returns `null` for a missing `InternalTable` instead of throwing, to avoid hard-failing export/clone on stale table references. The test still encodes the pre-change throwing contract. | fix-in-place |
| `tooljet-db/unit/tooljet-db-import-export.service.spec.ts` | `.bulkImport › should import multiple ToolJet DB tables with foreign key relationships` — `QueryFailedError: invalid input syntax for type uuid: "products-table-id"` | Test fixture uses the literal string `"products-table-id"` as a table id where the code path (`tooljet-db-import-export.service.ts:152`, via `SelectQueryBuilder.getOne`) builds a Postgres query against a `uuid`-typed column. Fixture data was never a valid UUID. | fix-in-place |
| `versions/unit/app-version.ability.spec.ts` | 5 failing tests under "MODULES resource type" (`grants all edit actions when isAllEditable is true`, `grants all edit actions when resourceId is in editableAppsId`, `denies edit actions when resourceId is not in editableAppsId`, `denies all actions with no permissions`) and "PROMOTE permission › grants PROMOTE via appPromote on MODULES resource type" | `defineAppVersionAbility` (`src/modules/versions/ability/app-version.ability.ts:22-25`) has an early-return branch: for `resourceType === MODULES.MODULES` and `!isAdmin && !superAdmin && !isBuilder`, it grants only `GET/GET_ONE/GET_EVENTS` and returns immediately — before the `isAllEditable`/`editableAppsId`/`appPromote` checks below ever run. The tests build permissions with `isBuilder` defaulted to `false` and expect the edit/promote checks to still apply (and, in "denies all actions with no permissions", even expect view actions to be denied) — contradicting the implementation's documented intent ("every authenticated user can read" a module, comment at line 18-21). Either the tests need `isBuilder: true` added to exercise the intended edit/promote branch and the view-denial assertion needs correcting to match the "always viewable" comment, or the early-return semantics themselves are the bug — this needs product/eng sign-off on which side is correct before editing. | needs-product-decision |
| `workflows/unit/python-sandbox-security.spec.ts` | 2 tests skipped in the baseline run; not caught by the brief's literal `grep -E '\.skip\(|xdescribe\(|xit\('` (no matches — that pattern requires a call-paren directly after `.skip`, and the file's usage is `describe.skip` referenced as a value, not called inline) | Lines 1188 and 1232 conditionally alias `describeSeccomp`/`describeCgroup` to `describe.skip` at module-load time when `securityCapabilities.seccomp.available` / `.cgroupv2.writable` are `false` — i.e. tests are gated on host sandboxing capability (seccomp, cgroupv2 write access), which this dev machine (macOS) lacks. This is an intentional, environment-gated skip, not dead/abandoned test code. | reviewed-ok |

**Hollow-assertion pass (Step 4):** all 20 remaining unit spec files not covered above were read in full — `app/unit/app.service.spec.ts`, `app/unit/private-app-auth.guard.spec.ts`, `app/unit/valid-app.guard.spec.ts`, `apps/unit/app-dto-validation.spec.ts`, `data-queries/unit/throttler-wiring.spec.ts`, `data-queries/unit/util.service.spec.ts`, `encryption/unit/encryption.service.spec.ts`, `folder-apps/unit/service.spec.ts`, `git-sync/unit/git-object-cache.service.spec.ts`, `group-permissions/unit/feature-ability.spec.ts`, `group-permissions/unit/group-existance.guard.spec.ts`, `login-configs/unit/util.service.spec.ts`, `organization-env/unit/registry.service.spec.ts`, `session/unit/session.service.spec.ts`, `users/unit/users.service.spec.ts`, `workflows/unit/javascript-bundle-generation.service.spec.ts`, `workflows/unit/npm-registry.service.spec.ts`, `workflows/unit/pypi-registry.service.spec.ts`, `workflows/unit/python-bundle-generation.service.spec.ts`, `workflows/unit/python-executor.service.spec.ts`, `workflows/unit/workflow-packages-ability.spec.ts`. No hollow assertions found in any of them (every `it` asserts specific values, thrown error types/messages, mock call arguments, or object shapes — including a few terse `toBeDefined()` calls that are always paired with a real value/shape check in the same block, not standalone). No rows added per the "omit files with no findings" instruction.
