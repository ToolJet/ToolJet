import { Injectable } from '@nestjs/common';

/**
 * CE stub for GroupAdminService.
 * Group admin functionality is an Enterprise-only feature.
 */
@Injectable()
export class GroupAdminService {
  // CE stub — all group-admin logic is EE-only.
  // EE overrides this via ee/group-permissions/services/group-admin.service.ts
}
