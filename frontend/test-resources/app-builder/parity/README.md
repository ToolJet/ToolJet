# App Builder parity resources

Independent expected values for alias, asset, edition, and JSX parity tests live here. Jest parity is report-only; production-build parity remains a separate execution lane.
