{
  "$schema": "https://json-schema.org/",
  "$id": "https://tooljet.io/Mysql.schema.json",
  "title": "Mysql datasource",
  "description": "A schema defining mysql datasource",
  "type": "object",
  "properties": {
    "host": {
      "$label": "Host",
      "$key": "host",
      "type": "text",
      "description": "Input for host"
    },
    "port": {
      "$label": "Port",
      "$key": "port",
      "type": "text",
      "description": "Input for port"
    },
    "database": {
      "$label": "Database Name",
      "$key": "database",
      "type": "text",
      "description": "Input for database"
    },
    "username": {
      "$label": "Username",
      "$key": "username",
      "type": "text",
      "description": "Input for username field"
    },
    "password": {
      "$label": "Password",
      "$key": "password",
      "type": "password",
      "description": "Input for password field"
    }
  },
  "required": ["host", "port", "username", "password"]
}
