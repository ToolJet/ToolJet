import React from 'react';
// import { resolveWidgetFieldValue } from '@/_helpers/utils';

export const FilePicker = function FilePicker({ id, component, onComponentClick, currentState }) {
  console.log(id, component, onComponentClick, currentState);
  return <div>File picker</div>;
};
