import { PageContainer as GenericPageContainer } from '@/features/commons/components';

// App-specific wrapper for backwards compatibility
export { GenericPageContainer as PageContainer };

export default GenericPageContainer;
