# Examples

Use `defineAppBuilderScenario` for every test and choose one primary seam. `AppBuilderTestSession` is the supported RTL/store entry point; direct Zustand mutation is intentionally not exposed.
