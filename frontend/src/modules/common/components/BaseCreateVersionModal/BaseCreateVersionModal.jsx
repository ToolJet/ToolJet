import React from 'react';
import CreateVersionModal from '@/AppBuilder/Header/CreateVersionModal';

const BaseCreateVersionModal = (props) => {
  return <CreateVersionModal {...props} />;
};

export default BaseCreateVersionModal;
