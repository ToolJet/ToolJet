/**
 * @tjType  toggle, switch
 * @tjBlock properties
 * @tjUsage annotatedHelper('Loading state', '{{false}}')
 * @tjDom   inspector fx-toggle + code editor
 */
export const annotatedHelper = (displayName, value) => {};
