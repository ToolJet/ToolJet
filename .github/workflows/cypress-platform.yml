name:
  Cypress Platform
  # Cache invalidation - 2025-12-09

on:
  pull_request_target:
    types: [labeled]
  # workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write

env:
  PR_NUMBER: ${{ github.event.number }}
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
  TIMESTAMP: ${{ github.run_number }}-${{ github.run_attempt }}

jobs:
  Cypress-Platform:
    runs-on: ubuntu-22.04
    outputs:
      test-summary-ce: ${{ steps.set-output-ce.outputs.summary }}
      test-summary-ee: ${{ steps.set-output-ee.outputs.summary }}

    if: contains(github.event.pull_request.labels.*.name, 'run-cypress') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-ce')

    strategy:
      fail-fast: false
      matrix:
        edition:
          - ${{
            (contains(github.event.pull_request.labels.*.name, 'run-cypress') ||
            contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce') ||
            contains(github.event.pull_request.labels.*.name, 'run-cypress-ce')) && 'ce' ||
            contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee') && 'ee' || ''
            }}
        exclude:
          - edition: ""

    steps:
      - name: Debug labels and matrix edition
        run: |
          echo "Labels: ${{ toJSON(github.event.pull_request.labels.*.name) }}"
          echo "Matrix edition: ${{ matrix.edition }}"

      - name: Free up disk space
        run: |
          echo "Available disk space before cleanup:"
          df -h

          # Remove unnecessary packages
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' '^php.*' '^mongodb-.*' '^mysql-.*' azure-cli google-cloud-sdk hhvm firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          sudo apt-get clean

          # Remove large directories
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean Docker
          docker system prune -af --volumes

          echo "Available disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Set DOCKER_CLI_EXPERIMENTAL
        run: echo "DOCKER_CLI_EXPERIMENTAL=enabled" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set SAFE_BRANCH_NAME
        run: echo "SAFE_BRANCH_NAME=$(echo ${{ env.BRANCH_NAME }} | tr '/' '-')" >> $GITHUB_ENV

      - name: Build CE Docker image
        if: matrix.edition == 'ce'
        run: |
          echo "Building CE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ce-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce \
            --no-cache \
            --load \
            .

          echo "Pushing CE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      # EE dockerfile build below
      - name: Build EE Docker image
        if: matrix.edition == 'ee'
        run: |
          echo "Building EE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f cypress-tests/cypress-lts.Dockerfile \
            --build-arg CUSTOM_GITHUB_TOKEN=${{ secrets.CUSTOM_GITHUB_TOKEN }} \
            --build-arg BRANCH_NAME=${{ github.event.pull_request.head.ref }} \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee \
            --no-cache \
            --load \
            .

          echo "Pushing EE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Set up environment variables
        run: |
          echo "TOOLJET_EDITION=${{ matrix.edition }}" >> .env
          echo "TOOLJET_HOST=http://localhost:3000" >> .env
          echo "LOCKBOX_MASTER_KEY=cd97331a419c09387bef49787f7da8d2a81d30733f0de6bed23ad8356d2068b2" >> .env
          echo "SECRET_KEY_BASE=7073b9a35a15dd20914ae17e36a693093f25b74b96517a5fec461fc901c51e011cd142c731bee48c5081ec8bac321c1f259ef097ef2a16f25df17a3798c03426" >> .env          
          echo "PG_DB=tooljet_development" >> .env
          echo "PG_USER=postgres" >> .env
          echo "PG_HOST=postgres" >> .env
          echo "PG_PASS=postgres" >> .env
          echo "PG_PORT=5432" >> .env
          echo "ENABLE_TOOLJET_DB=true" >> .env
          echo "TOOLJET_DB=tooljet_db" >> .env
          echo "TOOLJET_DB_USER=postgres" >> .env
          echo "TOOLJET_DB_HOST=postgres" >> .env
          echo "TOOLJET_DB_PASS=postgres" >> .env
          echo "TOOLJET_DB_STATEMENT_TIMEOUT=60000" >> .env
          echo "TOOLJET_DB_RECONFIG=true" >> .env
          echo "PGRST_JWT_SECRET=r9iMKoe5CRMgvJBBtp4HrqN7QiPpUToj" >> .env
          echo "PGRST_HOST=localhost:3001" >> .env
          echo "PGRST_DB_PRE_CONFIG=postgrest.pre_config" >> .env
          echo "PGRST_DB_URI=postgres://postgres:postgres@postgres/tooljet_db" >> .env
          echo "PGRST_SERVER_PORT=3001" >> .env
          echo "ENABLE_MARKETPLACE_FEATURE=true" >> .env
          echo "ENABLE_MARKETPLACE_DEV_MODE=true" >> .env
          echo "ENABLE_PRIVATE_APP_EMBED=true" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_ID=123456789.apps.googleusercontent.com" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_SECRET=ABCGFDNF-FHSDVFY-bskfh6234" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_ID=1234567890" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_SECRET=3346shfvkdjjsfkvxce32854e026a4531ed" >> .env
          echo "SSO_OPENID_NAME=tj-oidc-simulator" >> .env
          echo "SSO_OPENID_CLIENT_ID=${{ secrets.SSO_OPENID_CLIENT_ID }}" >> .env
          echo "SSO_OPENID_CLIENT_SECRET=${{ secrets.SSO_OPENID_CLIENT_SECRET }}" >> .env
          echo "ENABLE_EXTERNAL_API=true" >> .env
          echo "EXTERNAL_API_ACCESS_TOKEN=d980eb3af24d783991cee51a2d84dce9f9bd41d4b46f441cc691ccebbecd3cbc" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__development='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__development='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__staging='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__staging='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__production='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__production='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "SAML_SET_ENTITY_ID_REDIRECT_URL=true" >> .env

      - name: clean up old docker containers
        run: |
          docker system prune -af --volumes
          echo "Disk space after Docker cleanup:"
          df -h

      - name: Pulling the docker-compose file
        run: curl -LO https://tooljet-test.s3.us-west-1.amazonaws.com/docker-compose.yaml && mkdir postgres_data

      - name: Update docker-compose file
        run: |
          # Update docker-compose.yaml with the appropriate image based on edition
          if [ "${{ matrix.edition }}" = "ce" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce|' docker-compose.yaml
          elif [ "${{ matrix.edition }}" = "ee" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee|' docker-compose.yaml
          fi

      - name: view docker-compose file
        run: cat docker-compose.yaml

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.10.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run docker-compose file
        run: docker-compose up -d

      - name: Checking containers
        run: docker ps -a

      - name: sleep
        run: sleep 80

      - name: docker logs
        run: docker-compose logs tooljet

      - name: Wait for the server to be ready
        run: |
          echo "⏳ Waiting for ToolJet to start (timeout: 700 seconds)..."
          SUCCESS_FOUND=false
          TIMEOUT=700
          ELAPSED=0

          while [ $ELAPSED -lt $TIMEOUT ]; do
            # Check for success message in logs
            if docker-compose logs tooljet 2>/dev/null | grep -qE "🚀 TOOLJET APPLICATION STARTED SUCCESSFULLY|Ready to use at http://localhost:82 🚀|Ready to use at http://localhost:80"; then
              echo "✅ Found success message in logs!"
              SUCCESS_FOUND=true
              break
            fi

            echo "⏳ Still waiting... (${ELAPSED}s elapsed)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done

          if [ "$SUCCESS_FOUND" = false ]; then
            echo "❌ Timeout reached without finding success logs"
            echo "📄 Showing current logs for troubleshooting..."
            docker-compose logs --tail=100 tooljet
            exit 1
          fi

          echo "✅ Server is ready!"

      - name: Test database connection
        run: |
          # Wait for database to be ready
          echo "Testing database connection..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "SELECT current_database();"

      - name: Create delete_user procedure
        run: |
          echo "Creating delete_users stored procedure..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "
          CREATE OR REPLACE PROCEDURE delete_users(p_emails TEXT[])
          LANGUAGE plpgsql
          AS \$\$
          DECLARE
            v_email TEXT;
            v_user_id UUID;
            v_organization_ids UUID[] := ARRAY[]::UUID[];
            v_organizations_to_delete UUID[] := ARRAY[]::UUID[];
            v_log_message TEXT;
          BEGIN
            IF COALESCE(array_length(p_emails, 1), 0) = 0 THEN
              RAISE NOTICE 'delete_users: no emails provided';
              RETURN;
            END IF;

            FOREACH v_email IN ARRAY p_emails LOOP
              BEGIN
                RAISE NOTICE '========================================';
                RAISE NOTICE 'Starting user deletion for email: %', v_email;

                -- Fetch user id
                SELECT id INTO v_user_id
                FROM users
                WHERE email = v_email;

                IF v_user_id IS NULL THEN
                  RAISE NOTICE 'User with email % not found. Skipping.', v_email;
                  CONTINUE;
                END IF;

                RAISE NOTICE 'User found with id: %', v_user_id;

                -- Collect organization memberships
                SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                INTO v_organization_ids
                FROM organization_users
                WHERE user_id = v_user_id;

                RAISE NOTICE 'Found % organizations for user',
                  COALESCE(array_length(v_organization_ids, 1), 0);

                -- Find organizations with that single user
                IF array_length(v_organization_ids, 1) > 0 THEN
                  SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                  INTO v_organizations_to_delete
                  FROM (
                    SELECT organization_id
                    FROM organization_users
                    WHERE organization_id = ANY(v_organization_ids)
                    GROUP BY organization_id
                    HAVING COUNT(*) = 1
                  ) subquery;
                ELSE
                  v_organizations_to_delete := ARRAY[]::UUID[];
                END IF;

                RAISE NOTICE 'Found % organizations to delete',
                  COALESCE(array_length(v_organizations_to_delete, 1), 0);

                -- Cascade delete records for orgs slated for removal
                IF array_length(v_organizations_to_delete, 1) > 0 THEN
                  WITH deleted_apps AS (
                    DELETE FROM apps
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' apps'
                  INTO v_log_message FROM deleted_apps;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_data_sources AS (
                    DELETE FROM data_sources
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' data sources'
                  INTO v_log_message FROM deleted_data_sources;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_organizations AS (
                    DELETE FROM organizations
                    WHERE id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' organizations'
                  INTO v_log_message FROM deleted_organizations;
                  RAISE NOTICE '%', v_log_message;
                ELSE
                  RAISE NOTICE 'No organizations removed for user %', v_email;
                END IF;

                -- Delete audit logs for orgs (if any) and user
                WITH deleted_audit_logs AS (
                  DELETE FROM audit_logs
                  WHERE user_id = v_user_id
                     OR organization_id = ANY(v_organizations_to_delete)
                  RETURNING id
                )
                SELECT 'Deleted ' || COUNT(*) || ' audit logs'
                INTO v_log_message FROM deleted_audit_logs;
                RAISE NOTICE '%', v_log_message;

                -- Delete organization membership records
                DELETE FROM organization_users
                WHERE user_id = v_user_id;

                -- Delete the user
                DELETE FROM users
                WHERE id = v_user_id;

                RAISE NOTICE 'Deleted user with id: %', v_user_id;
                RAISE NOTICE 'User deletion completed for email: %', v_email;
              EXCEPTION
                WHEN OTHERS THEN
                  RAISE NOTICE 'Error deleting user %: %', v_email, SQLERRM;
                  -- continue with next email
              END;
            END LOOP;

            RAISE NOTICE '========================================';
            RAISE NOTICE 'delete_users procedure finished.';
          END;
          \$\$;"
          echo "✅ delete_users procedure created successfully"

      - name: Create Cypress environment file
        id: create-json-tj
        uses: jsdaniell/create-json@1.1.2
        with:
          name: "cypress.env.json"
          json: ${{ toJSON(matrix.edition == 'ce' && fromJSON(secrets.CYPRESS_SECRETS) || fromJSON(secrets.CYPRESS_EE_SECRETS)) }}
          dir: "./cypress-tests"

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        id: cypress-tests
        continue-on-error: true
        with:
          browser: chrome
          working-directory: ./cypress-tests
          config: "baseUrl=http://localhost:3000"
          config-file: ${{ matrix.edition == 'ee' && 'cypress-ee-platform.config.js' || 'cypress-platform.config.js' }}
        env:
          GITHUB_TOKEN: ${{ secrets.CYPRESS_RECORD_KEY }}
          CYPRESS_RECORD_KEY: "ca6a0d5f-b763-4be7-b554-3425a973104e"

      - name: Generate test summary
        if: always()
        id: test-summary
        run: |
          OUTCOME="${{ steps.cypress-tests.outcome }}"
          EDITION="${{ matrix.edition }}"

          # Try to find and parse test results
          TOTAL_TESTS=0
          PASSED_TESTS=0
          FAILED_TESTS=0
          SKIPPED_TESTS=0

          # Look for Cypress results in various formats
          if [ -f cypress-tests/results.json ]; then
            echo "Found results.json"
            RESULTS_FILE="cypress-tests/results.json"
          elif [ -f cypress-tests/cypress/results/output.json ]; then
            echo "Found output.json"
            RESULTS_FILE="cypress-tests/cypress/results/output.json"
          fi

          # Parse results if file exists
          if [ -n "$RESULTS_FILE" ] && [ -f "$RESULTS_FILE" ]; then
            echo "Parsing test results from $RESULTS_FILE"
            # Extract test counts using jq if available
            if command -v jq &> /dev/null; then
              TOTAL_TESTS=$(jq '.totalTests // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              PASSED_TESTS=$(jq '.totalPassed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              FAILED_TESTS=$(jq '.totalFailed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              SKIPPED_TESTS=$(jq '.totalPending // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
            fi
          fi

          # Create enhanced summary
          echo "## Cypress Platform Tests - ${EDITION^^} Edition" > summary.md
          echo "" >> summary.md
          echo "| Metric | Count |" >> summary.md
          echo "|--------|-------|" >> summary.md
          echo "| ✅ Passed | $PASSED_TESTS |" >> summary.md
          echo "| ❌ Failed | $FAILED_TESTS |" >> summary.md
          echo "| ⏭️ Skipped | $SKIPPED_TESTS |" >> summary.md
          echo "| 📊 **Total** | **$TOTAL_TESTS** |" >> summary.md

          # Save summary as multiline output
          {
            echo "summary<<EOFMARKER"
            cat summary.md
            echo "EOFMARKER"
          } >> $GITHUB_OUTPUT

      - name: Set output for CE
        if: always() && matrix.edition == 'ce'
        id: set-output-ce
        run: echo "Completed CE tests"

      - name: Set output for EE
        if: always() && matrix.edition == 'ee'
        id: set-output-ee
        run: echo "Completed EE tests"

      - name: Capture Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots-${{ matrix.edition }}
          path: cypress-tests/cypress/screenshots

  Cypress-Platform-Subpath:
    runs-on: ubuntu-22.04
    outputs:
      test-summary-subpath-ce: ${{ steps.set-output-subpath-ce.outputs.summary }}
      test-summary-subpath-ee: ${{ steps.set-output-subpath-ee.outputs.summary }}

    if: contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce-deployments') || contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee-deployments')

    strategy:
      fail-fast: false
      matrix:
        edition:
          - ${{ contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce-deployments') && 'ce' || contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee-deployments') && 'ee' || '' }}
        exclude:
          - edition: ""

    steps:
      - name: Debug labels and matrix edition
        run: |
          echo "Labels: ${{ toJSON(github.event.pull_request.labels.*.name) }}"
          echo "Matrix edition: ${{ matrix.edition }}"

      - name: Free up disk space
        run: |
          echo "Available disk space before cleanup:"
          df -h

          # Remove unnecessary packages
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' '^php.*' '^mongodb-.*' '^mysql-.*' azure-cli google-cloud-sdk hhvm firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          sudo apt-get clean

          # Remove large directories
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean Docker
          docker system prune -af --volumes

          echo "Available disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Set DOCKER_CLI_EXPERIMENTAL
        run: echo "DOCKER_CLI_EXPERIMENTAL=enabled" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set SAFE_BRANCH_NAME
        run: echo "SAFE_BRANCH_NAME=$(echo ${{ env.BRANCH_NAME }} | tr '/' '-')" >> $GITHUB_ENV

      - name: Build CE Docker image
        if: matrix.edition == 'ce'
        run: |
          echo "Building CE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ce-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce \
            --no-cache \
            --load \
            .

          echo "Pushing CE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Build EE Docker image
        if: matrix.edition == 'ee'
        run: |
          echo "Building EE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f cypress-tests/cypress-lts.Dockerfile \
            --build-arg CUSTOM_GITHUB_TOKEN=${{ secrets.CUSTOM_GITHUB_TOKEN }} \
            --build-arg BRANCH_NAME=${{ github.event.pull_request.head.ref }} \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee \
            --no-cache \
            --load \
            .

          echo "Pushing EE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Set up environment variables
        run: |
          echo "TOOLJET_EDITION=${{ matrix.edition }}" >> .env
          echo "TOOLJET_HOST=http://localhost:3000" >> .env
          echo "SUB_PATH=/apps/" >> .env
          echo "LOCKBOX_MASTER_KEY=cd97331a419c09387bef49787f7da8d2a81d30733f0de6bed23ad8356d2068b2" >> .env
          echo "SECRET_KEY_BASE=7073b9a35a15dd20914ae17e36a693093f25b74b96517a5fec461fc901c51e011cd142c731bee48c5081ec8bac321c1f259ef097ef2a16f25df17a3798c03426" >> .env          
          echo "PG_DB=tooljet_development" >> .env
          echo "PG_USER=postgres" >> .env
          echo "PG_HOST=postgres" >> .env
          echo "PG_PASS=postgres" >> .env
          echo "PG_PORT=5432" >> .env
          echo "ENABLE_TOOLJET_DB=true" >> .env
          echo "TOOLJET_DB=tooljet_db" >> .env
          echo "TOOLJET_DB_USER=postgres" >> .env
          echo "TOOLJET_DB_HOST=postgres" >> .env
          echo "TOOLJET_DB_PASS=postgres" >> .env
          echo "TOOLJET_DB_STATEMENT_TIMEOUT=60000" >> .env
          echo "TOOLJET_DB_RECONFIG=true" >> .env
          echo "PGRST_JWT_SECRET=r9iMKoe5CRMgvJBBtp4HrqN7QiPpUToj" >> .env
          echo "PGRST_HOST=localhost:3001" >> .env
          echo "PGRST_DB_PRE_CONFIG=postgrest.pre_config" >> .env
          echo "PGRST_DB_URI=postgres://postgres:postgres@postgres/tooljet_db" >> .env
          echo "PGRST_SERVER_PORT=3001" >> .env
          echo "ENABLE_MARKETPLACE_FEATURE=true" >> .env
          echo "ENABLE_MARKETPLACE_DEV_MODE=true" >> .env
          echo "ENABLE_PRIVATE_APP_EMBED=true" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_ID=123456789.apps.googleusercontent.com" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_SECRET=ABCGFDNF-FHSDVFY-bskfh6234" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_ID=1234567890" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_SECRET=3346shfvkdjjsfkvxce32854e026a4531ed" >> .env
          echo "SSO_OPENID_NAME=tj-oidc-simulator" >> .env
          echo "SSO_OPENID_CLIENT_ID=${{ secrets.SSO_OPENID_CLIENT_ID }}" >> .env
          echo "SSO_OPENID_CLIENT_SECRET=${{ secrets.SSO_OPENID_CLIENT_SECRET }}" >> .env
          echo "ENABLE_EXTERNAL_API=true" >> .env
          echo "EXTERNAL_API_ACCESS_TOKEN=d980eb3af24d783991cee51a2d84dce9f9bd41d4b46f441cc691ccebbecd3cbc" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__development='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__development='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__staging='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__staging='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__production='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__production='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "SAML_SET_ENTITY_ID_REDIRECT_URL=true" >> .env

      - name: clean up old docker containers
        run: |
          docker system prune -af --volumes
          echo "Disk space after Docker cleanup:"
          df -h

      - name: Pulling the docker-compose file
        run: curl -LO https://tooljet-test.s3.us-west-1.amazonaws.com/docker-compose.yaml && mkdir postgres_data

      - name: Update docker-compose file
        run: |
          # Update docker-compose.yaml with the appropriate image based on edition
          if [ "${{ matrix.edition }}" = "ce" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce|' docker-compose.yaml
          elif [ "${{ matrix.edition }}" = "ee" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee|' docker-compose.yaml
          fi

      - name: view docker-compose file
        run: cat docker-compose.yaml

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.10.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run docker-compose file
        run: docker-compose up -d

      - name: Checking containers
        run: docker ps -a

      - name: sleep
        run: sleep 80

      - name: docker logs
        run: docker-compose logs tooljet

      - name: Wait for the server to be ready
        run: |
          echo "⏳ Waiting for ToolJet to start (timeout: 700 seconds)..."
          SUCCESS_FOUND=false
          TIMEOUT=700
          ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            # Check for success message in logs
            if docker-compose logs tooljet 2>/dev/null | grep -qE "🚀 TOOLJET APPLICATION STARTED SUCCESSFULLY"; then
              echo "✅ Found success message in logs!"
              SUCCESS_FOUND=true
              break
            fi
            echo "⏳ Still waiting... (${ELAPSED}s elapsed)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          if [ "$SUCCESS_FOUND" = false ]; then
            echo "❌ Timeout reached without finding success logs"
            echo "📄 Showing current logs for troubleshooting..."
            docker-compose logs --tail=100 tooljet
            exit 1
          fi
          echo "✅ Server is ready!"

      - name: Test database connection
        run: |
          # Wait for database to be ready
          echo "Testing database connection..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "SELECT current_database();"

      - name: Create delete_user procedure
        run: |
          echo "Creating delete_users stored procedure..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "
          CREATE OR REPLACE PROCEDURE delete_users(p_emails TEXT[])
          LANGUAGE plpgsql
          AS \$\$
          DECLARE
            v_email TEXT;
            v_user_id UUID;
            v_organization_ids UUID[] := ARRAY[]::UUID[];
            v_organizations_to_delete UUID[] := ARRAY[]::UUID[];
            v_log_message TEXT;
          BEGIN
            IF COALESCE(array_length(p_emails, 1), 0) = 0 THEN
              RAISE NOTICE 'delete_users: no emails provided';
              RETURN;
            END IF;

            FOREACH v_email IN ARRAY p_emails LOOP
              BEGIN
                RAISE NOTICE '========================================';
                RAISE NOTICE 'Starting user deletion for email: %', v_email;

                -- Fetch user id
                SELECT id INTO v_user_id
                FROM users
                WHERE email = v_email;

                IF v_user_id IS NULL THEN
                  RAISE NOTICE 'User with email % not found. Skipping.', v_email;
                  CONTINUE;
                END IF;

                RAISE NOTICE 'User found with id: %', v_user_id;

                -- Collect organization memberships
                SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                INTO v_organization_ids
                FROM organization_users
                WHERE user_id = v_user_id;

                RAISE NOTICE 'Found % organizations for user',
                  COALESCE(array_length(v_organization_ids, 1), 0);

                -- Find organizations with that single user
                IF array_length(v_organization_ids, 1) > 0 THEN
                  SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                  INTO v_organizations_to_delete
                  FROM (
                    SELECT organization_id
                    FROM organization_users
                    WHERE organization_id = ANY(v_organization_ids)
                    GROUP BY organization_id
                    HAVING COUNT(*) = 1
                  ) subquery;
                ELSE
                  v_organizations_to_delete := ARRAY[]::UUID[];
                END IF;

                RAISE NOTICE 'Found % organizations to delete',
                  COALESCE(array_length(v_organizations_to_delete, 1), 0);

                -- Cascade delete records for orgs slated for removal
                IF array_length(v_organizations_to_delete, 1) > 0 THEN
                  WITH deleted_apps AS (
                    DELETE FROM apps
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' apps'
                  INTO v_log_message FROM deleted_apps;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_data_sources AS (
                    DELETE FROM data_sources
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' data sources'
                  INTO v_log_message FROM deleted_data_sources;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_organizations AS (
                    DELETE FROM organizations
                    WHERE id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' organizations'
                  INTO v_log_message FROM deleted_organizations;
                  RAISE NOTICE '%', v_log_message;
                ELSE
                  RAISE NOTICE 'No organizations removed for user %', v_email;
                END IF;

                -- Delete audit logs for orgs (if any) and user
                WITH deleted_audit_logs AS (
                  DELETE FROM audit_logs
                  WHERE user_id = v_user_id
                     OR organization_id = ANY(v_organizations_to_delete)
                  RETURNING id
                )
                SELECT 'Deleted ' || COUNT(*) || ' audit logs'
                INTO v_log_message FROM deleted_audit_logs;
                RAISE NOTICE '%', v_log_message;

                -- Delete organization membership records
                DELETE FROM organization_users
                WHERE user_id = v_user_id;

                -- Delete the user
                DELETE FROM users
                WHERE id = v_user_id;

                RAISE NOTICE 'Deleted user with id: %', v_user_id;
                RAISE NOTICE 'User deletion completed for email: %', v_email;
              EXCEPTION
                WHEN OTHERS THEN
                  RAISE NOTICE 'Error deleting user %: %', v_email, SQLERRM;
                  -- continue with next email
              END;
            END LOOP;

            RAISE NOTICE '========================================';
            RAISE NOTICE 'delete_users procedure finished.';
          END;
          \$\$;"
          echo "✅ delete_users procedure created successfully"

      - name: Create Cypress environment file
        id: create-json-tj-subpath
        uses: jsdaniell/create-json@1.1.2
        with:
          name: "cypress.env.json"
          json: ${{ toJSON(matrix.edition == 'ce' && fromJSON(secrets.CYPRESS_SUBPATH_SECRETS) || fromJSON(secrets.CYPRESS_EE_SUBPATH_SECRETS)) }}
          dir: "./cypress-tests"

      - name: Verify Chrome is available
        run: |
          echo "Checking Chrome installation..."
          which google-chrome-stable || which google-chrome || echo "Chrome not found in PATH"
          google-chrome --version || google-chrome-stable --version || echo "Could not get Chrome version"

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        id: cypress-tests-subpath
        continue-on-error: true
        with:
          browser: chrome
          working-directory: ./cypress-tests
          config: "baseUrl=http://localhost:3000/apps"
          config-file: ${{ matrix.edition == 'ee' && 'cypress-ee-platform.config.js' || 'cypress-platform.config.js' }}
        env:
          GITHUB_TOKEN: ${{ secrets.CYPRESS_RECORD_KEY }}
          CYPRESS_RECORD_KEY: "ca6a0d5f-b763-4be7-b554-3425a973104e"

      - name: Generate test summary
        if: always()
        id: test-summary-subpath
        run: |
          OUTCOME="${{ steps.cypress-tests-subpath.outcome }}"
          EDITION="${{ matrix.edition }}"

          # Try to find and parse test results
          TOTAL_TESTS=0
          PASSED_TESTS=0
          FAILED_TESTS=0
          SKIPPED_TESTS=0

          # Look for Cypress results in various formats
          if [ -f cypress-tests/results.json ]; then
            echo "Found results.json"
            RESULTS_FILE="cypress-tests/results.json"
          elif [ -f cypress-tests/cypress/results/output.json ]; then
            echo "Found output.json"
            RESULTS_FILE="cypress-tests/cypress/results/output.json"
          fi

          # Parse results if file exists
          if [ -n "$RESULTS_FILE" ] && [ -f "$RESULTS_FILE" ]; then
            echo "Parsing test results from $RESULTS_FILE"
            # Extract test counts using jq if available
            if command -v jq &> /dev/null; then
              TOTAL_TESTS=$(jq '.totalTests // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              PASSED_TESTS=$(jq '.totalPassed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              FAILED_TESTS=$(jq '.totalFailed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              SKIPPED_TESTS=$(jq '.totalPending // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
            fi
          fi

          # Create simple summary
          echo "### Cypress Tests - ${EDITION^^} Edition (Subpath)" > summary.md
          echo "" >> summary.md
          echo "| Total | Passed | Failed | Skipped |" >> summary.md
          echo "|-------|--------|--------|---------|" >> summary.md
          echo "| $TOTAL_TESTS | $PASSED_TESTS | $FAILED_TESTS | $SKIPPED_TESTS |" >> summary.md

          # Save summary as multiline output
          {
            echo "summary<<EOFMARKER"
            cat summary.md
            echo "EOFMARKER"
          } >> $GITHUB_OUTPUT

      - name: Set output for Subpath CE
        if: always() && matrix.edition == 'ce'
        id: set-output-subpath-ce
        run: echo "Completed Subpath CE tests"

      - name: Set output for Subpath EE
        if: always() && matrix.edition == 'ee'
        id: set-output-subpath-ee
        run: echo "Completed Subpath EE tests"

      - name: Capture Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots-${{ matrix.edition }}-subpath-${{ env.TIMESTAMP }}
          path: cypress-tests/cypress/screenshots

  Cypress-Platform-Proxy:
    runs-on: ubuntu-22.04
    outputs:
      test-summary-proxy-ce: ${{ steps.set-output-proxy-ce.outputs.summary }}
      test-summary-proxy-ee: ${{ steps.set-output-proxy-ee.outputs.summary }}

    if: |
      contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce-deployments') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee-deployments')

    strategy:
      fail-fast: false
      matrix:
        edition:
          - ${{ contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce-deployments') && 'ce' || '' }}
          - ${{ contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee-deployments') && 'ee' || '' }}
        exclude:
          - edition: ""

    steps:
      - name: Debug labels and matrix edition
        run: |
          echo "Labels: ${{ toJSON(github.event.pull_request.labels.*.name) }}"
          echo "Matrix edition: ${{ matrix.edition }}"

      - name: Free up disk space
        run: |
          echo "Available disk space before cleanup:"
          df -h

          # Remove unnecessary packages
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' '^php.*' '^mongodb-.*' '^mysql-.*' azure-cli google-cloud-sdk hhvm firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          sudo apt-get clean

          # Remove large directories
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean Docker
          docker system prune -af --volumes

          echo "Available disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Set DOCKER_CLI_EXPERIMENTAL
        run: echo "DOCKER_CLI_EXPERIMENTAL=enabled" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set SAFE_BRANCH_NAME
        run: echo "SAFE_BRANCH_NAME=$(echo ${{ env.BRANCH_NAME }} | tr '/' '-')" >> $GITHUB_ENV

      - name: Build CE Docker image
        if: matrix.edition == 'ce'
        run: |
          echo "Building CE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ce-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce \
            --no-cache \
            --load \
            .

          echo "Pushing CE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Build EE Docker image
        if: matrix.edition == 'ee'
        run: |
          echo "Building EE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f cypress-tests/cypress-lts.Dockerfile \
            --build-arg CUSTOM_GITHUB_TOKEN=${{ secrets.CUSTOM_GITHUB_TOKEN }} \
            --build-arg BRANCH_NAME=${{ github.event.pull_request.head.ref }} \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee \
            --no-cache \
            --load \
            .

          echo "Pushing EE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Set up environment variables
        run: |
          echo "TOOLJET_EDITION=${{ matrix.edition }}" >> .env
          echo "TOOLJET_HOST=http://localhost:3000" >> .env
          echo "LOCKBOX_MASTER_KEY=cd97331a419c09387bef49787f7da8d2a81d30733f0de6bed23ad8356d2068b2" >> .env
          echo "SECRET_KEY_BASE=7073b9a35a15dd20914ae17e36a693093f25b74b96517a5fec461fc901c51e011cd142c731bee48c5081ec8bac321c1f259ef097ef2a16f25df17a3798c03426" >> .env
          echo "PG_DB=tooljet_development" >> .env
          echo "PG_USER=postgres" >> .env
          echo "PG_HOST=postgres" >> .env
          echo "PG_PASS=postgres" >> .env
          echo "PG_PORT=5432" >> .env
          echo "ENABLE_TOOLJET_DB=true" >> .env
          echo "TOOLJET_DB=tooljet_db" >> .env
          echo "TOOLJET_DB_USER=postgres" >> .env
          echo "TOOLJET_DB_HOST=postgres" >> .env
          echo "TOOLJET_DB_PASS=postgres" >> .env
          echo "PGRST_JWT_SECRET=r9iMKoe5CRMgvJBBtp4HrqN7QiPpUToj" >> .env
          echo "PGRST_HOST=localhost:3001" >> .env
          echo "ENABLE_MARKETPLACE_FEATURE=true" >> .env
          echo "ENABLE_MARKETPLACE_DEV_MODE=true" >> .env
          echo "ENABLE_PRIVATE_APP_EMBED=true" >> .env
          echo "TOOLJET_DB_STATEMENT_TIMEOUT=60000" >> .env
          echo "TOOLJET_DB_RECONFIG=true" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_ID=123456789.apps.googleusercontent.com" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_SECRET=ABCGFDNF-FHSDVFY-bskfh6234" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_ID=1234567890" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_SECRET=3346shfvkdjjsfkvxce32854e026a4531ed" >> .env
          echo "PGRST_DB_PRE_CONFIG=postgrest.pre_config" >> .env
          echo "PGRST_DB_URI=postgres://postgres:postgres@postgres/tooljet_db" >> .env
          echo "PGRST_SERVER_PORT=3001" >> .env
          echo "SSO_OPENID_NAME=tj-oidc-simulator" >> .env
          echo "SSO_OPENID_CLIENT_ID=${{ secrets.SSO_OPENID_CLIENT_ID }}" >> .env
          echo "SSO_OPENID_CLIENT_SECRET=${{ secrets.SSO_OPENID_CLIENT_SECRET }}" >> .env
          echo "ENABLE_EXTERNAL_API=true" >> .env
          echo "EXTERNAL_API_ACCESS_TOKEN=d980eb3af24d783991cee51a2d84dce9f9bd41d4b46f441cc691ccebbecd3cbc" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__development='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__development='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__staging='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__staging='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__production='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__production='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "SAML_SET_ENTITY_ID_REDIRECT_URL=true" >> .env

      - name: clean up old docker containers
        run: |
          docker system prune -af --volumes
          echo "Disk space after Docker cleanup:"
          df -h

      - name: Pulling the docker-compose file
        run: curl -LO https://tooljet-test.s3.us-west-1.amazonaws.com/docker-compose.yaml && mkdir postgres_data

      - name: Update docker-compose file
        run: |
          # Update docker-compose.yaml with the appropriate image based on edition
          if [ "${{ matrix.edition }}" = "ce" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce|' docker-compose.yaml
          elif [ "${{ matrix.edition }}" = "ee" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee|' docker-compose.yaml
          fi

      - name: view docker-compose file
        run: cat docker-compose.yaml

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.10.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run docker-compose file
        run: docker-compose up -d

      - name: Checking containers
        run: docker ps -a

      - name: sleep
        run: sleep 80

      - name: docker logs
        run: docker-compose logs tooljet

      - name: Setup Nginx
        run: |
          sudo apt update
          sudo apt install -y nginx
          sudo systemctl stop apache2 || true
          sudo apt remove apache2 -y || true
          sudo cp cypress-tests/proxy.nginx /etc/nginx/sites-available/nginx-config
          sudo ln -sf /etc/nginx/sites-available/nginx-config /etc/nginx/sites-enabled/nginx-config
          sudo nginx -t
          sudo systemctl start nginx
          sudo systemctl reload nginx
          sudo netstat -tulpn | grep 4001

      - name: Wait for the server to be ready
        run: |
          echo "⏳ Waiting for ToolJet to start (timeout: 700 seconds)..."
          SUCCESS_FOUND=false
          TIMEOUT=700
          ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            # Check for success message in logs
            if docker-compose logs tooljet 2>/dev/null | grep -qE "🚀 TOOLJET APPLICATION STARTED SUCCESSFULLY"; then
              echo "✅ Found success message in logs!"
              SUCCESS_FOUND=true
              break
            fi
            echo "⏳ Still waiting... (${ELAPSED}s elapsed)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          if [ "$SUCCESS_FOUND" = false ]; then
            echo "❌ Timeout reached without finding success logs"
            echo "📄 Showing current logs for troubleshooting..."
            docker-compose logs --tail=100 tooljet
            exit 1
          fi
          echo "✅ Server is ready!"

      - name: Test database connection
        run: |
          # Wait for database to be ready
          echo "Testing database connection..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "SELECT current_database();"

      - name: Create delete_user procedure
        run: |
          echo "Creating delete_users stored procedure..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "
          CREATE OR REPLACE PROCEDURE delete_users(p_emails TEXT[])
          LANGUAGE plpgsql
          AS \$\$
          DECLARE
            v_email TEXT;
            v_user_id UUID;
            v_organization_ids UUID[] := ARRAY[]::UUID[];
            v_organizations_to_delete UUID[] := ARRAY[]::UUID[];
            v_log_message TEXT;
          BEGIN
            IF COALESCE(array_length(p_emails, 1), 0) = 0 THEN
              RAISE NOTICE 'delete_users: no emails provided';
              RETURN;
            END IF;

            FOREACH v_email IN ARRAY p_emails LOOP
              BEGIN
                RAISE NOTICE '========================================';
                RAISE NOTICE 'Starting user deletion for email: %', v_email;

                -- Fetch user id
                SELECT id INTO v_user_id
                FROM users
                WHERE email = v_email;

                IF v_user_id IS NULL THEN
                  RAISE NOTICE 'User with email % not found. Skipping.', v_email;
                  CONTINUE;
                END IF;

                RAISE NOTICE 'User found with id: %', v_user_id;

                -- Collect organization memberships
                SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                INTO v_organization_ids
                FROM organization_users
                WHERE user_id = v_user_id;

                RAISE NOTICE 'Found % organizations for user',
                  COALESCE(array_length(v_organization_ids, 1), 0);

                -- Find organizations with that single user
                IF array_length(v_organization_ids, 1) > 0 THEN
                  SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                  INTO v_organizations_to_delete
                  FROM (
                    SELECT organization_id
                    FROM organization_users
                    WHERE organization_id = ANY(v_organization_ids)
                    GROUP BY organization_id
                    HAVING COUNT(*) = 1
                  ) subquery;
                ELSE
                  v_organizations_to_delete := ARRAY[]::UUID[];
                END IF;

                RAISE NOTICE 'Found % organizations to delete',
                  COALESCE(array_length(v_organizations_to_delete, 1), 0);

                -- Cascade delete records for orgs slated for removal
                IF array_length(v_organizations_to_delete, 1) > 0 THEN
                  WITH deleted_apps AS (
                    DELETE FROM apps
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' apps'
                  INTO v_log_message FROM deleted_apps;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_data_sources AS (
                    DELETE FROM data_sources
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' data sources'
                  INTO v_log_message FROM deleted_data_sources;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_organizations AS (
                    DELETE FROM organizations
                    WHERE id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' organizations'
                  INTO v_log_message FROM deleted_organizations;
                  RAISE NOTICE '%', v_log_message;
                ELSE
                  RAISE NOTICE 'No organizations removed for user %', v_email;
                END IF;

                -- Delete audit logs for orgs (if any) and user
                WITH deleted_audit_logs AS (
                  DELETE FROM audit_logs
                  WHERE user_id = v_user_id
                     OR organization_id = ANY(v_organizations_to_delete)
                  RETURNING id
                )
                SELECT 'Deleted ' || COUNT(*) || ' audit logs'
                INTO v_log_message FROM deleted_audit_logs;
                RAISE NOTICE '%', v_log_message;

                -- Delete organization membership records
                DELETE FROM organization_users
                WHERE user_id = v_user_id;

                -- Delete the user
                DELETE FROM users
                WHERE id = v_user_id;

                RAISE NOTICE 'Deleted user with id: %', v_user_id;
                RAISE NOTICE 'User deletion completed for email: %', v_email;
              EXCEPTION
                WHEN OTHERS THEN
                  RAISE NOTICE 'Error deleting user %: %', v_email, SQLERRM;
                  -- continue with next email
              END;
            END LOOP;

            RAISE NOTICE '========================================';
            RAISE NOTICE 'delete_users procedure finished.';
          END;
          \$\$;"
          echo "✅ delete_users procedure created successfully"

      - name: Create Cypress environment file
        id: create-json-tj-proxy
        uses: jsdaniell/create-json@1.1.2
        with:
          name: "cypress.env.json"
          json: ${{ toJSON(matrix.edition == 'ce' && fromJSON(secrets.CYPRESS_PROXY_SECRETS) || fromJSON(secrets.CYPRESS_EE_PROXY_SECRETS)) }}
          dir: "./cypress-tests"

      - name: Verify Chrome is available
        run: |
          echo "Checking Chrome installation..."
          which google-chrome-stable || which google-chrome || echo "Chrome not found in PATH"
          google-chrome --version || google-chrome-stable --version || echo "Could not get Chrome version"

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        id: cypress-tests-proxy
        continue-on-error: true
        with:
          browser: chrome
          working-directory: ./cypress-tests
          config: "baseUrl=http://localhost:4001,server_host=http://localhost:3000"
          config-file: ${{ matrix.edition == 'ee' && 'cypress-ee-platform.config.js' || 'cypress-platform.config.js' }}
        env:
          CYPRESS_proxy: true
          GITHUB_TOKEN: ${{ secrets.CYPRESS_RECORD_KEY }}
          CYPRESS_RECORD_KEY: "ca6a0d5f-b763-4be7-b554-3425a973104e"

      - name: Generate test summary
        if: always()
        id: test-summary-proxy
        run: |
          OUTCOME="${{ steps.cypress-tests-proxy.outcome }}"
          EDITION="${{ matrix.edition }}"

          # Try to find and parse test results
          TOTAL_TESTS=0
          PASSED_TESTS=0
          FAILED_TESTS=0
          SKIPPED_TESTS=0

          # Look for Cypress results in various formats
          if [ -f cypress-tests/results.json ]; then
            echo "Found results.json"
            RESULTS_FILE="cypress-tests/results.json"
          elif [ -f cypress-tests/cypress/results/output.json ]; then
            echo "Found output.json"
            RESULTS_FILE="cypress-tests/cypress/results/output.json"
          fi

          # Parse results if file exists
          if [ -n "$RESULTS_FILE" ] && [ -f "$RESULTS_FILE" ]; then
            echo "Parsing test results from $RESULTS_FILE"
            # Extract test counts using jq if available
            if command -v jq &> /dev/null; then
              TOTAL_TESTS=$(jq '.totalTests // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              PASSED_TESTS=$(jq '.totalPassed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              FAILED_TESTS=$(jq '.totalFailed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              SKIPPED_TESTS=$(jq '.totalPending // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
            fi
          fi

          # Create simple summary
          echo "### Cypress Tests - ${EDITION^^} Edition (Proxy)" > summary.md
          echo "" >> summary.md
          echo "| Total | Passed | Failed | Skipped |" >> summary.md
          echo "|-------|--------|--------|---------|" >> summary.md
          echo "| $TOTAL_TESTS | $PASSED_TESTS | $FAILED_TESTS | $SKIPPED_TESTS |" >> summary.md

          # Save summary as multiline output
          {
            echo "summary<<EOFMARKER"
            cat summary.md
            echo "EOFMARKER"
          } >> $GITHUB_OUTPUT

      - name: Set output for Proxy CE
        if: always() && matrix.edition == 'ce'
        id: set-output-proxy-ce
        run: echo "Completed Proxy CE tests"

      - name: Set output for Proxy EE
        if: always() && matrix.edition == 'ee'
        id: set-output-proxy-ee
        run: echo "Completed Proxy EE tests"

      - name: Capture Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots-${{ matrix.edition }}-proxy-${{ env.TIMESTAMP }}
          path: cypress-tests/cypress/screenshots

  Cypress-Platform-Proxy-Subpath:
    runs-on: ubuntu-22.04
    outputs:
      test-summary-proxy-subpath-ce: ${{ steps.set-output-proxy-subpath-ce.outputs.summary }}
      test-summary-proxy-subpath-ee: ${{ steps.set-output-proxy-subpath-ee.outputs.summary }}

    if: |
      contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce-deployments') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee-deployments')

    strategy:
      fail-fast: false
      matrix:
        edition:
          - ${{ contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ce-deployments') && 'ce' || '' }}
          - ${{ contains(github.event.pull_request.labels.*.name, 'run-cypress-platform-ee-deployments') && 'ee' || '' }}
        exclude:
          - edition: ""

    steps:
      - name: Debug labels and matrix edition
        run: |
          echo "Labels: ${{ toJSON(github.event.pull_request.labels.*.name) }}"
          echo "Matrix edition: ${{ matrix.edition }}"

      - name: Free up disk space
        run: |
          echo "Available disk space before cleanup:"
          df -h

          # Remove unnecessary packages
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' '^php.*' '^mongodb-.*' '^mysql-.*' azure-cli google-cloud-sdk hhvm firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          sudo apt-get clean

          # Remove large directories
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean Docker
          docker system prune -af --volumes

          echo "Available disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Set DOCKER_CLI_EXPERIMENTAL
        run: echo "DOCKER_CLI_EXPERIMENTAL=enabled" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set SAFE_BRANCH_NAME
        run: echo "SAFE_BRANCH_NAME=$(echo ${{ env.BRANCH_NAME }} | tr '/' '-')" >> $GITHUB_ENV

      - name: Build CE Docker image
        if: matrix.edition == 'ce'
        run: |
          echo "Building CE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ce-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce \
            --no-cache \
            --load \
            .

          echo "Pushing CE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Build EE Docker image
        if: matrix.edition == 'ee'
        run: |
          echo "Building EE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f cypress-tests/cypress-lts.Dockerfile \
            --build-arg CUSTOM_GITHUB_TOKEN=${{ secrets.CUSTOM_GITHUB_TOKEN }} \
            --build-arg BRANCH_NAME=${{ github.event.pull_request.head.ref }} \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee \
            --no-cache \
            --load \
            .

          echo "Pushing EE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Set up environment variables
        run: |
          echo "TOOLJET_EDITION=${{ matrix.edition }}" >> .env
          echo "TOOLJET_HOST=http://localhost:3000" >> .env
          echo "SUB_PATH=/apps/" >> .env
          echo "LOCKBOX_MASTER_KEY=cd97331a419c09387bef49787f7da8d2a81d30733f0de6bed23ad8356d2068b2" >> .env
          echo "SECRET_KEY_BASE=7073b9a35a15dd20914ae17e36a693093f25b74b96517a5fec461fc901c51e011cd142c731bee48c5081ec8bac321c1f259ef097ef2a16f25df17a3798c03426" >> .env          
          echo "PG_DB=tooljet_development" >> .env
          echo "PG_USER=postgres" >> .env
          echo "PG_HOST=postgres" >> .env
          echo "PG_PASS=postgres" >> .env
          echo "PG_PORT=5432" >> .env
          echo "ENABLE_TOOLJET_DB=true" >> .env
          echo "TOOLJET_DB=tooljet_db" >> .env
          echo "TOOLJET_DB_USER=postgres" >> .env
          echo "TOOLJET_DB_HOST=postgres" >> .env
          echo "TOOLJET_DB_PASS=postgres" >> .env
          echo "TOOLJET_DB_STATEMENT_TIMEOUT=60000" >> .env
          echo "TOOLJET_DB_RECONFIG=true" >> .env
          echo "PGRST_JWT_SECRET=r9iMKoe5CRMgvJBBtp4HrqN7QiPpUToj" >> .env
          echo "PGRST_HOST=localhost:3001" >> .env
          echo "PGRST_DB_PRE_CONFIG=postgrest.pre_config" >> .env
          echo "PGRST_DB_URI=postgres://postgres:postgres@postgres/tooljet_db" >> .env
          echo "PGRST_SERVER_PORT=3001" >> .env
          echo "ENABLE_MARKETPLACE_FEATURE=true" >> .env
          echo "ENABLE_MARKETPLACE_DEV_MODE=true" >> .env
          echo "ENABLE_PRIVATE_APP_EMBED=true" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_ID=123456789.apps.googleusercontent.com" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_SECRET=ABCGFDNF-FHSDVFY-bskfh6234" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_ID=1234567890" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_SECRET=3346shfvkdjjsfkvxce32854e026a4531ed" >> .env
          echo "SSO_OPENID_NAME=tj-oidc-simulator" >> .env
          echo "SSO_OPENID_CLIENT_ID=${{ secrets.SSO_OPENID_CLIENT_ID }}" >> .env
          echo "SSO_OPENID_CLIENT_SECRET=${{ secrets.SSO_OPENID_CLIENT_SECRET }}" >> .env
          echo "ENABLE_EXTERNAL_API=true" >> .env
          echo "EXTERNAL_API_ACCESS_TOKEN=d980eb3af24d783991cee51a2d84dce9f9bd41d4b46f441cc691ccebbecd3cbc" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__development='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__development='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/development\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__staging='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__staging='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/staging\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_GLOBAL_CONSTANTS__production='{\"envConstant\":\"globalUI\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "TOOLJET_SECRET_CONSTANTS__production='{\"envSecret\":\"secret\",\"headerKey\":\"customHeader\",\"ui_url\":\"http://20.29.40.108:4000/production\",\"headerValue\":\"key=value\"}'" >> .env
          echo "SAML_SET_ENTITY_ID_REDIRECT_URL=true" >> .env

      - name: clean up old docker containers
        run: |
          docker system prune -af --volumes
          echo "Disk space after Docker cleanup:"
          df -h

      - name: Pulling the docker-compose file
        run: curl -LO https://tooljet-test.s3.us-west-1.amazonaws.com/docker-compose.yaml && mkdir postgres_data

      - name: Update docker-compose file
        run: |
          # Update docker-compose.yaml with the appropriate image based on edition
          if [ "${{ matrix.edition }}" = "ce" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce|' docker-compose.yaml
          elif [ "${{ matrix.edition }}" = "ee" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee|' docker-compose.yaml
          fi

      - name: view docker-compose file
        run: cat docker-compose.yaml

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.10.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run docker-compose file
        run: docker-compose up -d

      - name: Checking containers
        run: docker ps -a

      - name: sleep
        run: sleep 80

      - name: docker logs
        run: docker-compose logs tooljet

      - name: Setup Nginx
        run: |
          sudo apt update
          sudo apt install -y nginx
          sudo systemctl stop apache2 || true
          sudo apt remove apache2 -y || true
          sudo cp cypress-tests/subpath.nginx /etc/nginx/sites-available/nginx-config
          sudo ln -sf /etc/nginx/sites-available/nginx-config /etc/nginx/sites-enabled/nginx-config
          sudo nginx -t
          sudo systemctl start nginx
          sudo systemctl reload nginx
          sudo netstat -tulpn | grep 4001

      - name: Wait for the server to be ready
        run: |
          echo "⏳ Waiting for ToolJet to start (timeout: 700 seconds)..."
          SUCCESS_FOUND=false
          TIMEOUT=700
          ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            # Check for success message in logs
            if docker-compose logs tooljet 2>/dev/null | grep -qE "🚀 TOOLJET APPLICATION STARTED SUCCESSFULLY"; then
              echo "✅ Found success message in logs!"
              SUCCESS_FOUND=true
              break
            fi
            echo "⏳ Still waiting... (${ELAPSED}s elapsed)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          if [ "$SUCCESS_FOUND" = false ]; then
            echo "❌ Timeout reached without finding success logs"
            echo "📄 Showing current logs for troubleshooting..."
            docker-compose logs --tail=100 tooljet
            exit 1
          fi
          echo "✅ Server is ready!"

      - name: Test database connection
        run: |
          # Wait for database to be ready
          echo "Testing database connection..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "SELECT current_database();"

      - name: Create delete_user procedure
        run: |
          echo "Creating delete_users stored procedure..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "
          CREATE OR REPLACE PROCEDURE delete_users(p_emails TEXT[])
          LANGUAGE plpgsql
          AS \$\$
          DECLARE
            v_email TEXT;
            v_user_id UUID;
            v_organization_ids UUID[] := ARRAY[]::UUID[];
            v_organizations_to_delete UUID[] := ARRAY[]::UUID[];
            v_log_message TEXT;
          BEGIN
            IF COALESCE(array_length(p_emails, 1), 0) = 0 THEN
              RAISE NOTICE 'delete_users: no emails provided';
              RETURN;
            END IF;

            FOREACH v_email IN ARRAY p_emails LOOP
              BEGIN
                RAISE NOTICE '========================================';
                RAISE NOTICE 'Starting user deletion for email: %', v_email;

                -- Fetch user id
                SELECT id INTO v_user_id
                FROM users
                WHERE email = v_email;

                IF v_user_id IS NULL THEN
                  RAISE NOTICE 'User with email % not found. Skipping.', v_email;
                  CONTINUE;
                END IF;

                RAISE NOTICE 'User found with id: %', v_user_id;

                -- Collect organization memberships
                SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                INTO v_organization_ids
                FROM organization_users
                WHERE user_id = v_user_id;

                RAISE NOTICE 'Found % organizations for user',
                  COALESCE(array_length(v_organization_ids, 1), 0);

                -- Find organizations with that single user
                IF array_length(v_organization_ids, 1) > 0 THEN
                  SELECT COALESCE(ARRAY_AGG(organization_id), ARRAY[]::UUID[])
                  INTO v_organizations_to_delete
                  FROM (
                    SELECT organization_id
                    FROM organization_users
                    WHERE organization_id = ANY(v_organization_ids)
                    GROUP BY organization_id
                    HAVING COUNT(*) = 1
                  ) subquery;
                ELSE
                  v_organizations_to_delete := ARRAY[]::UUID[];
                END IF;

                RAISE NOTICE 'Found % organizations to delete',
                  COALESCE(array_length(v_organizations_to_delete, 1), 0);

                -- Cascade delete records for orgs slated for removal
                IF array_length(v_organizations_to_delete, 1) > 0 THEN
                  WITH deleted_apps AS (
                    DELETE FROM apps
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' apps'
                  INTO v_log_message FROM deleted_apps;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_data_sources AS (
                    DELETE FROM data_sources
                    WHERE organization_id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' data sources'
                  INTO v_log_message FROM deleted_data_sources;
                  RAISE NOTICE '%', v_log_message;

                  WITH deleted_organizations AS (
                    DELETE FROM organizations
                    WHERE id = ANY(v_organizations_to_delete)
                    RETURNING id
                  )
                  SELECT 'Deleted ' || COUNT(*) || ' organizations'
                  INTO v_log_message FROM deleted_organizations;
                  RAISE NOTICE '%', v_log_message;
                ELSE
                  RAISE NOTICE 'No organizations removed for user %', v_email;
                END IF;

                -- Delete audit logs for orgs (if any) and user
                WITH deleted_audit_logs AS (
                  DELETE FROM audit_logs
                  WHERE user_id = v_user_id
                     OR organization_id = ANY(v_organizations_to_delete)
                  RETURNING id
                )
                SELECT 'Deleted ' || COUNT(*) || ' audit logs'
                INTO v_log_message FROM deleted_audit_logs;
                RAISE NOTICE '%', v_log_message;

                -- Delete organization membership records
                DELETE FROM organization_users
                WHERE user_id = v_user_id;

                -- Delete the user
                DELETE FROM users
                WHERE id = v_user_id;

                RAISE NOTICE 'Deleted user with id: %', v_user_id;
                RAISE NOTICE 'User deletion completed for email: %', v_email;
              EXCEPTION
                WHEN OTHERS THEN
                  RAISE NOTICE 'Error deleting user %: %', v_email, SQLERRM;
                  -- continue with next email
              END;
            END LOOP;

            RAISE NOTICE '========================================';
            RAISE NOTICE 'delete_users procedure finished.';
          END;
          \$\$;"
          echo "✅ delete_users procedure created successfully"

      - name: Create Cypress environment file
        id: create-json-tj-proxy-subpath
        uses: jsdaniell/create-json@1.1.2
        with:
          name: "cypress.env.json"
          json: ${{ toJSON(matrix.edition == 'ce' && fromJSON(secrets.CYPRESS_SUBPATH_PROXY_SECRETS) || fromJSON(secrets.CYPRESS_EE_SUBPATH_PROXY_SECRETS)) }}
          dir: "./cypress-tests"

      - name: Verify Chrome is available
        run: |
          echo "Checking Chrome installation..."
          which google-chrome-stable || which google-chrome || echo "Chrome not found in PATH"
          google-chrome --version || google-chrome-stable --version || echo "Could not get Chrome version"

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        id: cypress-tests-proxy-subpath
        continue-on-error: true
        with:
          browser: chrome
          working-directory: ./cypress-tests
          config: "baseUrl=http://localhost:4001/apps,server_host=http://localhost:3000/apps"
          config-file: ${{ matrix.edition == 'ee' && 'cypress-ee-platform.config.js' || 'cypress-platform.config.js' }}
        env:
          CYPRESS_proxy: true
          GITHUB_TOKEN: ${{ secrets.CYPRESS_RECORD_KEY }}
          CYPRESS_RECORD_KEY: "ca6a0d5f-b763-4be7-b554-3425a973104e"

      - name: Generate test summary
        if: always()
        id: test-summary-proxy-subpath
        run: |
          OUTCOME="${{ steps.cypress-tests-proxy-subpath.outcome }}"
          EDITION="${{ matrix.edition }}"

          # Try to find and parse test results
          TOTAL_TESTS=0
          PASSED_TESTS=0
          FAILED_TESTS=0
          SKIPPED_TESTS=0

          # Look for Cypress results in various formats
          if [ -f cypress-tests/results.json ]; then
            echo "Found results.json"
            RESULTS_FILE="cypress-tests/results.json"
          elif [ -f cypress-tests/cypress/results/output.json ]; then
            echo "Found output.json"
            RESULTS_FILE="cypress-tests/cypress/results/output.json"
          fi

          # Parse results if file exists
          if [ -n "$RESULTS_FILE" ] && [ -f "$RESULTS_FILE" ]; then
            echo "Parsing test results from $RESULTS_FILE"
            # Extract test counts using jq if available
            if command -v jq &> /dev/null; then
              TOTAL_TESTS=$(jq '.totalTests // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              PASSED_TESTS=$(jq '.totalPassed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              FAILED_TESTS=$(jq '.totalFailed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              SKIPPED_TESTS=$(jq '.totalPending // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
            fi
          fi

          # Create simple summary
          echo "### Cypress Tests - ${EDITION^^} Edition (Proxy + Subpath)" > summary.md
          echo "" >> summary.md
          echo "| Total | Passed | Failed | Skipped |" >> summary.md
          echo "|-------|--------|--------|---------|" >> summary.md
          echo "| $TOTAL_TESTS | $PASSED_TESTS | $FAILED_TESTS | $SKIPPED_TESTS |" >> summary.md

          # Save summary as multiline output
          {
            echo "summary<<EOFMARKER"
            cat summary.md
            echo "EOFMARKER"
          } >> $GITHUB_OUTPUT

      - name: Set output for Proxy-Subpath CE
        if: always() && matrix.edition == 'ce'
        id: set-output-proxy-subpath-ce
        run: echo "Completed Proxy-Subpath CE tests"

      - name: Set output for Proxy-Subpath EE
        if: always() && matrix.edition == 'ee'
        id: set-output-proxy-subpath-ee
        run: echo "Completed Proxy-Subpath EE tests"

      - name: Capture Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots-${{ matrix.edition }}-proxy-subpath-${{ env.TIMESTAMP }}
          path: cypress-tests/cypress/screenshots
          retention-days: 7

  create-platform-coverage-comment:
    if: always() && github.event_name == 'pull_request_target'
    needs: [Cypress-Platform, Cypress-Platform-Subpath, Cypress-Platform-Proxy, Cypress-Platform-Proxy-Subpath]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write

    steps:
      - name: Create Platform Test Results Comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 🎯 Cypress Platform Test Coverage

            ### Base Deployment

            ${{ needs.Cypress-Platform.outputs.test-summary-ce || '_(CE tests not run)_' }}

            ---

            ${{ needs.Cypress-Platform.outputs.test-summary-ee || '_(EE tests not run)_' }}

            ---

            ### Subpath Deployment

            ${{ needs.Cypress-Platform-Subpath.outputs.test-summary-subpath-ce || '_(CE tests not run)_' }}

            ---

            ${{ needs.Cypress-Platform-Subpath.outputs.test-summary-subpath-ee || '_(EE tests not run)_' }}

            ---

            ### Proxy Deployment

            ${{ needs.Cypress-Platform-Proxy.outputs.test-summary-proxy-ce || '_(CE tests not run)_' }}

            ---

            ${{ needs.Cypress-Platform-Proxy.outputs.test-summary-proxy-ee || '_(EE tests not run)_' }}

            ---

            ### Proxy + Subpath Deployment

            ${{ needs.Cypress-Platform-Proxy-Subpath.outputs.test-summary-proxy-subpath-ce || '_(CE tests not run)_' }}

            ---

            ${{ needs.Cypress-Platform-Proxy-Subpath.outputs.test-summary-proxy-subpath-ee || '_(EE tests not run)_' }}
          edit-mode: replace

      - name: Add Job Summary
        if: always()
        run: |
          echo "## 🎯 Cypress Platform Test Coverage" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### Base Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Base CE summary if available
          BASE_CE='${{ needs.Cypress-Platform.outputs.test-summary-ce }}'
          if [ -n "$BASE_CE" ] && [ "$BASE_CE" != "" ]; then
            echo "$BASE_CE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(CE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Base EE summary if available
          BASE_EE='${{ needs.Cypress-Platform.outputs.test-summary-ee }}'
          if [ -n "$BASE_EE" ] && [ "$BASE_EE" != "" ]; then
            echo "$BASE_EE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(EE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Subpath Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Subpath CE summary if available
          SUBPATH_CE='${{ needs.Cypress-Platform-Subpath.outputs.test-summary-subpath-ce }}'
          if [ -n "$SUBPATH_CE" ] && [ "$SUBPATH_CE" != "" ]; then
            echo "$SUBPATH_CE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(CE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Subpath EE summary if available
          SUBPATH_EE='${{ needs.Cypress-Platform-Subpath.outputs.test-summary-subpath-ee }}'
          if [ -n "$SUBPATH_EE" ] && [ "$SUBPATH_EE" != "" ]; then
            echo "$SUBPATH_EE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(EE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Proxy Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Proxy CE summary if available
          PROXY_CE='${{ needs.Cypress-Platform-Proxy.outputs.test-summary-proxy-ce }}'
          if [ -n "$PROXY_CE" ] && [ "$PROXY_CE" != "" ]; then
            echo "$PROXY_CE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(CE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Proxy EE summary if available
          PROXY_EE='${{ needs.Cypress-Platform-Proxy.outputs.test-summary-proxy-ee }}'
          if [ -n "$PROXY_EE" ] && [ "$PROXY_EE" != "" ]; then
            echo "$PROXY_EE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(EE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Proxy + Subpath Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Proxy-Subpath CE summary if available
          PROXY_SUBPATH_CE='${{ needs.Cypress-Platform-Proxy-Subpath.outputs.test-summary-proxy-subpath-ce }}'
          if [ -n "$PROXY_SUBPATH_CE" ] && [ "$PROXY_SUBPATH_CE" != "" ]; then
            echo "$PROXY_SUBPATH_CE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(CE tests not run)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add Proxy-Subpath EE summary if available
          PROXY_SUBPATH_EE='${{ needs.Cypress-Platform-Proxy-Subpath.outputs.test-summary-proxy-subpath-ee }}'
          if [ -n "$PROXY_SUBPATH_EE" ] && [ "$PROXY_SUBPATH_EE" != "" ]; then
            echo "$PROXY_SUBPATH_EE" >> $GITHUB_STEP_SUMMARY
          else
            echo "_(EE tests not run)_" >> $GITHUB_STEP_SUMMARY
          fi
