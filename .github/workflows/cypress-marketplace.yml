name: Cypress Marketplace

on:
  pull_request_target:
    types: [labeled]

  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write

env:
  PR_NUMBER: ${{ github.event.number }}
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}

jobs:
  Cypress-Marketplace:
    runs-on: ubuntu-22.04

    if: contains(github.event.pull_request.labels.*.name, 'run-cypress') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-marketplace-ce') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-marketplace-ee') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-ce')

    strategy:
      matrix:
        edition: >-
          ${{
            contains(github.event.pull_request.labels.*.name, 'run-cypress') && fromJson('["ce", "ee"]') ||
            contains(github.event.pull_request.labels.*.name, 'run-cypress-ce') && fromJson('["ce"]') ||
            contains(github.event.pull_request.labels.*.name, 'run-cypress-marketplace-ce') && fromJson('["ce"]') ||
            contains(github.event.pull_request.labels.*.name, 'run-cypress-marketplace-ee') && fromJson('["ee"]') ||
            fromJson('[]')
          }}

    steps:
      - name: Free up disk space
        run: |
          echo "Available disk space before cleanup:"
          df -h

          # Remove unnecessary packages
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' '^php.*' '^mongodb-.*' '^mysql-.*' azure-cli google-cloud-sdk hhvm firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          sudo apt-get clean

          # Remove large directories
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean Docker
          docker system prune -af --volumes

          echo "Available disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Set DOCKER_CLI_EXPERIMENTAL
        run: echo "DOCKER_CLI_EXPERIMENTAL=enabled" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set SAFE_BRANCH_NAME
        run: echo "SAFE_BRANCH_NAME=$(echo ${{ env.BRANCH_NAME }} | tr '/' '-')" >> $GITHUB_ENV

      - name: Build CE Docker image
        if: matrix.edition == 'ce'
        run: |
          echo "Building CE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ce-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce \
            --no-cache \
            --load \
            .

          echo "Pushing CE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Build EE Docker image
        if: matrix.edition == 'ee'
        run: |
          echo "Building EE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ee/ee-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee \
            --no-cache \
            --load \
            .

          echo "Pushing EE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Set up environment variables
        run: |
          echo "TOOLJET_EDITION=${{ matrix.edition }}" >> .env
          echo "TOOLJET_HOST=http://localhost:3000" >> .env
          echo "LOCKBOX_MASTER_KEY=cd97331a419c09387bef49787f7da8d2a81d30733f0de6bed23ad8356d2068b2" >> .env
          echo "SECRET_KEY_BASE=7073b9a35a15dd20914ae17e36a693093f25b74b96517a5fec461fc901c51e011cd142c731bee48c5081ec8bac321c1f259ef097ef2a16f25df17a3798c03426" >> .env
          echo "PG_DB=tooljet_development" >> .env
          echo "PG_USER=postgres" >> .env
          echo "PG_HOST=postgres" >> .env
          echo "PG_PASS=postgres" >> .env
          echo "PG_PORT=5432" >> .env
          echo "ENABLE_TOOLJET_DB=true" >> .env
          echo "PGRST_DB_PRE_CONFIG=postgrest.pre_config" >> .env
          echo "TOOLJET_DB=tooljet_db" >> .env
          echo "TOOLJET_DB_USER=postgres" >> .env
          echo "TOOLJET_DB_HOST=postgres" >> .env
          echo "TOOLJET_DB_PASS=postgres" >> .env
          echo "PGRST_JWT_SECRET=r9iMKoe5CRMgvJBBtp4HrqN7QiPpUToj" >> .env
          echo "PGRST_HOST=postgrest" >> .env
          echo "PGRST_DB_URI=postgres://postgres:postgres@postgres/tooljet_db" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_ID=dummy" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_SECRET=dummy" >> .env
          echo "SSO_GIT_OAUTH2_HOST=dummy" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_ID=dummy" >> .env
          echo "ENABLE_MARKETPLACE_FEATURE=true" >> .env
          echo "LICENSE_KEY=${{ secrets.RENDER_LICENSE_KEY }}" >> .env

      - name: clean up old docker containers
        run: |
          docker system prune -af --volumes
          echo "Disk space after Docker cleanup:"
          df -h

      - name: Pulling the docker-compose file
        run: curl -LO https://tooljet-test.s3.us-west-1.amazonaws.com/docker-compose.yaml && mkdir postgres_data

      - name: Update docker-compose file
        run: |
          # Update docker-compose.yaml with the appropriate image based on edition
          if [ "${{ matrix.edition }}" = "ce" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce|' docker-compose.yaml
          elif [ "${{ matrix.edition }}" = "ee" ]; then
            sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ee|' docker-compose.yaml
          fi

      - name: view docker-compose file
        run: cat docker-compose.yaml

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.10.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run docker-compose file
        run: docker-compose up -d

      - name: Checking containers
        run: docker ps -a

      - name: sleep
        run: sleep 80

      - name: docker logs
        run: docker-compose logs tooljet

      - name: Wait for the server to be ready
        run: |
          echo "⏳ Waiting for ToolJet to start (timeout: 700 seconds)..."
          SUCCESS_FOUND=false
          TIMEOUT=700
          ELAPSED=0

          while [ $ELAPSED -lt $TIMEOUT ]; do
            # Check for success message in logs
            if docker-compose logs tooljet 2>/dev/null | grep -qE "🚀 TOOLJET APPLICATION STARTED SUCCESSFULLY|Ready to use at http://localhost:82 🚀|Ready to use at http://localhost:80"; then
              echo "✅ Found success message in logs!"
              SUCCESS_FOUND=true
              break
            fi

            echo "⏳ Still waiting... (${ELAPSED}s elapsed)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done

          if [ "$SUCCESS_FOUND" = false ]; then
            echo "❌ Timeout reached without finding success logs"
            echo "📄 Showing current logs for troubleshooting..."
            docker-compose logs --tail=100 tooljet
            exit 1
          fi

          echo "✅ Server is ready!"

      - name: Test database connection
        run: |
          # Wait for database to be ready
          echo "Testing database connection..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "SELECT current_database();"

      - name: Seeding (Setup Super Admin)
        run: |
          curl 'http://localhost:3000/api/onboarding/setup-super-admin' \
            -H 'Content-Type: application/json' \
            --data-raw '{
              "companyName": "ToolJet",
              "name": "The Developer",
              "workspaceName": "My workspace",
              "email": "dev@tooljet.io",
              "password": "password"
            }'

      - name: Create Cypress environment file
        id: create-json
        uses: jsdaniell/create-json@1.1.2
        with:
          name: "cypress.env.json"
          json: ${{ secrets.CYPRESS_SECRETS_MARKETPLACE }}
          dir: "./cypress-tests"

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        id: cypress-tests
        continue-on-error: true
        with:
          browser: chrome
          working-directory: ./cypress-tests
          config: "baseUrl=http://localhost:3000"
          config-file: cypress-marketplace.config.js
        env:
          GITHUB_TOKEN: ${{ secrets.CYPRESS_RECORD_KEY }}
          CYPRESS_RECORD_KEY: "ca6a0d5f-b763-4be7-b554-3425a973104e"

      - name: Generate test summary
        if: always()
        id: test-summary
        run: |
          OUTCOME="${{ steps.cypress-tests.outcome }}"
          EDITION="${{ matrix.edition }}"

          # Try to find and parse test results
          TOTAL_TESTS=0
          PASSED_TESTS=0
          FAILED_TESTS=0
          SKIPPED_TESTS=0

          # Look for Cypress results in various formats
          if [ -f cypress-tests/results.json ]; then
            echo "Found results.json"
            RESULTS_FILE="cypress-tests/results.json"
          elif [ -f cypress-tests/cypress/results/output.json ]; then
            echo "Found output.json"
            RESULTS_FILE="cypress-tests/cypress/results/output.json"
          fi

          # Parse results if file exists
          if [ -n "$RESULTS_FILE" ] && [ -f "$RESULTS_FILE" ]; then
            echo "Parsing test results from $RESULTS_FILE"
            # Extract test counts using jq if available
            if command -v jq &> /dev/null; then
              TOTAL_TESTS=$(jq '.totalTests // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              PASSED_TESTS=$(jq '.totalPassed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              FAILED_TESTS=$(jq '.totalFailed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              SKIPPED_TESTS=$(jq '.totalPending // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
            fi
          fi

          # Create simple summary
          echo "### Cypress Marketplace Tests - ${EDITION^^} Edition" > summary.md
          echo "" >> summary.md
          echo "| Total | Passed | Failed | Skipped |" >> summary.md
          echo "|-------|--------|--------|---------|" >> summary.md
          echo "| $TOTAL_TESTS | $PASSED_TESTS | $FAILED_TESTS | $SKIPPED_TESTS |" >> summary.md

          # Save for later use
          cat summary.md > /tmp/test-summary.md

      - name: Comment test results on PR
        if: always()
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body-path: /tmp/test-summary.md
          edit-mode: append

      - name: Capture Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots-${{ matrix.edition }}
          path: cypress-tests/cypress/screenshots

  Cypress-Marketplace-Subpath:
    runs-on: ubuntu-22.04

    if: contains(github.event.pull_request.labels.*.name, 'run-cypress') ||
      contains(github.event.pull_request.labels.*.name, 'run-cypress-marketplace-subpath')

    steps:
      - name: Free up disk space
        run: |
          echo "Available disk space before cleanup:"
          df -h

          # Remove unnecessary packages
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' '^php.*' '^mongodb-.*' '^mysql-.*' azure-cli google-cloud-sdk hhvm firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          sudo apt-get clean

          # Remove large directories
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean Docker
          docker system prune -af --volumes

          echo "Available disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Set DOCKER_CLI_EXPERIMENTAL
        run: echo "DOCKER_CLI_EXPERIMENTAL=enabled" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set SAFE_BRANCH_NAME
        run: echo "SAFE_BRANCH_NAME=$(echo ${{ env.BRANCH_NAME }} | tr '/' '-')" >> $GITHUB_ENV

      - name: Build CE Docker image
        run: |
          echo "Building CE Docker image..."
          docker buildx build \
            --platform=linux/amd64 \
            -f docker/ce-production.Dockerfile \
            -t tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce \
            --no-cache \
            --load \
            .

          echo "Pushing CE Docker image..."
          docker push tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce

          echo "Cleaning up build cache..."
          docker builder prune -af

          echo "Disk space after build:"
          df -h

      - name: Set up environment variables
        run: |
          echo "TOOLJET_HOST=http://localhost:80" >> .env
          echo "LOCKBOX_MASTER_KEY=cd97331a419c09387bef49787f7da8d2a81d30733f0de6bed23ad8356d2068b2" >> .env
          echo "SECRET_KEY_BASE=7073b9a35a15dd20914ae17e36a693093f25b74b96517a5fec461fc901c51e011cd142c731bee48c5081ec8bac321c1f259ef097ef2a16f25df17a3798c03426" >> .env
          echo "PG_DB=tooljet_development" >> .env
          echo "PG_USER=postgres" >> .env
          echo "PG_HOST=postgres" >> .env
          echo "PG_PASS=postgres" >> .env
          echo "PG_PORT=5432" >> .env
          echo "ENABLE_TOOLJET_DB=true" >> .env
          echo "TOOLJET_DB=tooljet_db" >> .env
          echo "TOOLJET_DB_USER=postgres" >> .env
          echo "TOOLJET_DB_HOST=postgres" >> .env
          echo "TOOLJET_DB_PASS=postgres" >> .env
          echo "PGRST_JWT_SECRET=r9iMKoe5CRMgvJBBtp4HrqN7QiPpUToj" >> .env
          echo "PGRST_HOST=postgrest" >> .env
          echo "PGRST_DB_URI=postgres://postgres:postgres@postgres/tooljet_db" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_ID=dummy" >> .env
          echo "SSO_GIT_OAUTH2_CLIENT_SECRET=dummy" >> .env
          echo "SSO_GIT_OAUTH2_HOST=dummy" >> .env
          echo "SSO_GOOGLE_OAUTH2_CLIENT_ID=dummy" >> .env
          echo "SUB_PATH=/apps/tooljet/" >> .env
          echo "NODE_ENV=production" >> .env
          echo "SERVE_CLIENT=true" >> .env
          echo "LICENSE_KEY=${{ secrets.RENDER_LICENSE_KEY }}" >> .env

      - name: clean up old docker containers
        run: |
          docker system prune -af --volumes
          echo "Disk space after Docker cleanup:"
          df -h

      - name: Pulling the docker-compose file
        run: curl -LO https://tooljet-test.s3.us-west-1.amazonaws.com/docker-compose.yaml && mkdir postgres_data

      - name: Update docker-compose file
        run: |
          # Update docker-compose.yaml with the CE image
          sed -i '/^[[:space:]]*tooljet:/,/^$/ s|^\([[:space:]]*image:[[:space:]]*\).*|\1tooljet/tj-osv:${{ env.SAFE_BRANCH_NAME }}-ce|' docker-compose.yaml

      - name: view docker-compose file
        run: cat docker-compose.yaml

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.10.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run docker-compose file
        run: docker-compose up -d

      - name: Checking containers
        run: docker ps -a

      - name: sleep
        run: sleep 80

      - name: docker logs
        run: docker-compose logs tooljet

      - name: Wait for the server to be ready
        run: |
          echo "⏳ Waiting for ToolJet to start (timeout: 700 seconds)..."
          SUCCESS_FOUND=false
          TIMEOUT=700
          ELAPSED=0

          while [ $ELAPSED -lt $TIMEOUT ]; do
            # Check for success message in logs
            if docker-compose logs tooljet 2>/dev/null | grep -qE "🚀 TOOLJET APPLICATION STARTED SUCCESSFULLY"; then
              echo "✅ Found success message in logs!"
              SUCCESS_FOUND=true
              break
            fi

            echo "⏳ Still waiting... (${ELAPSED}s elapsed)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done

          if [ "$SUCCESS_FOUND" = false ]; then
            echo "❌ Timeout reached without finding success logs"
            echo "📄 Showing current logs for troubleshooting..."
            docker-compose logs --tail=100 tooljet
            exit 1
          fi

          echo "✅ Server is ready!"

      - name: Test database connection
        run: |
          # Wait for database to be ready
          echo "Testing database connection..."
          docker-compose exec -T postgres psql -U postgres -d tooljet_development -c "SELECT current_database();"

      - name: Seeding
        run: docker exec Tooljet-app npm run db:seed:prod

      - name: Create Cypress environment file
        id: create-json
        uses: jsdaniell/create-json@1.1.2
        with:
          name: "cypress.env.json"
          json: ${{ secrets.CYPRESS_SECRETS }}
          dir: "./cypress-tests"

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        id: cypress-tests-subpath
        continue-on-error: true
        with:
          browser: chrome
          working-directory: ./cypress-tests
          config: "baseUrl=http://localhost:80/apps/tooljet/"
          config-file: cypress-marketplace.config.js
        env:
          GITHUB_TOKEN: ${{ secrets.CYPRESS_RECORD_KEY }}
          CYPRESS_RECORD_KEY: "ca6a0d5f-b763-4be7-b554-3425a973104e"

      - name: Generate test summary
        if: always()
        id: test-summary-subpath
        run: |
          OUTCOME="${{ steps.cypress-tests-subpath.outcome }}"

          # Try to find and parse test results
          TOTAL_TESTS=0
          PASSED_TESTS=0
          FAILED_TESTS=0
          SKIPPED_TESTS=0

          # Look for Cypress results in various formats
          if [ -f cypress-tests/results.json ]; then
            echo "Found results.json"
            RESULTS_FILE="cypress-tests/results.json"
          elif [ -f cypress-tests/cypress/results/output.json ]; then
            echo "Found output.json"
            RESULTS_FILE="cypress-tests/cypress/results/output.json"
          fi

          # Parse results if file exists
          if [ -n "$RESULTS_FILE" ] && [ -f "$RESULTS_FILE" ]; then
            echo "Parsing test results from $RESULTS_FILE"
            # Extract test counts using jq if available
            if command -v jq &> /dev/null; then
              TOTAL_TESTS=$(jq '.totalTests // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              PASSED_TESTS=$(jq '.totalPassed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              FAILED_TESTS=$(jq '.totalFailed // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
              SKIPPED_TESTS=$(jq '.totalPending // 0' "$RESULTS_FILE" 2>/dev/null || echo 0)
            fi
          fi

          # Create simple summary
          echo "### Cypress Marketplace Tests - CE Edition (Subpath)" > summary.md
          echo "" >> summary.md
          echo "| Total | Passed | Failed | Skipped |" >> summary.md
          echo "|-------|--------|--------|---------|" >> summary.md
          echo "| $TOTAL_TESTS | $PASSED_TESTS | $FAILED_TESTS | $SKIPPED_TESTS |" >> summary.md

          # Save for later use
          cat summary.md > /tmp/test-summary.md

      - name: Comment test results on PR
        if: always()
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body-path: /tmp/test-summary.md
          edit-mode: append

      - name: Capture Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots-ce-subpath
          path: cypress-tests/cypress/screenshots
